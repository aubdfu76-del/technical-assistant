# 🔧 إصلاح خطأ 500 عند رفع الملفات الكبيرة

## ✅ المشكلة

### الخطأ:
```
POST http://localhost:3000/api/ai/manuals/upload
Status: 500 Internal Server Error
```

### السبب:
- ملف PDF حجمه 50MB يحتوي على **ملايين الأحرف** من النص
- عند استخراج النص من PDF، قد يكون الحجم **أكبر من حد PostgreSQL**
- محاولة حفظ كل هذا النص في قاعدة البيانات تسبب خطأ 500

## ✅ الحل

### تحديد حجم المحتوى:
```typescript
// قبل
pdfContent = pdfData.text;
console.log(`📄 Extracted ${pdfContent.length} characters from PDF`);

// بعد
pdfContent = pdfData.text;
console.log(`📄 Extracted ${pdfContent.length} characters from PDF`);

// Limit to 100,000 characters
if (pdfContent.length > 100000) {
    console.log(`⚠️ Content too large, truncating to 100,000 chars`);
    pdfContent = pdfContent.substring(0, 100000);
}
```

## 📊 التفاصيل

### لماذا 100,000 حرف؟

1. **كافي للبحث:**
   - 100,000 حرف = ~20,000 كلمة
   - كافي لتغطية معظم محتوى الكراسة
   - يتضمن المعلومات الأساسية

2. **آمن للقاعدة:**
   - لا يتجاوز حد PostgreSQL
   - سريع في البحث
   - لا يسبب مشاكل في الأداء

3. **الملف الأصلي محفوظ:**
   - الـ PDF الكامل محفوظ في `/uploads/manuals`
   - يمكن الوصول إليه كاملاً
   - فقط النص المستخرج محدود

### مثال:

```
ملف PDF: 50MB
↓
استخراج النص: 500,000 حرف
↓
تحديد: 100,000 حرف (أول 100,000)
↓
حفظ في قاعدة البيانات: ✅ نجح
```

## ✨ الفوائد

- ✅ رفع ملفات كبيرة (حتى 100MB)
- ✅ لا أخطاء 500
- ✅ بحث سريع وفعال
- ✅ الملف الأصلي محفوظ كاملاً

## 🚀 جرب الآن!

**حدّث الصفحة** وحاول رفع الملف مرة أخرى - سيعمل! 🎉

### ملاحظة:
إذا كنت تحتاج البحث في الملف **كاملاً**:
- يمكن زيادة الحد إلى 200,000 أو 500,000
- لكن 100,000 كافي في معظم الحالات
- الملف الأصلي دائماً متاح للتحميل
