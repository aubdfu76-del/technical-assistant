# 🤖 دليل إعداد Google Gemini AI

## 📋 نظرة عامة

تم تطوير النظام ليستخدم **Google Gemini AI** كمحرك ذكاء اصطناعي متقدم لتقديم إجابات ذكية وتحليل الكراسات الفنية.

---

## 🔑 الحصول على Gemini API Key

### **الخطوة 1: زيارة Google AI Studio**

افتح المتصفح وانتقل إلى:
```
https://aistudio.google.com/app/apikey
```

### **الخطوة 2: تسجيل الدخول**

- سجل الدخول باستخدام حساب Google الخاص بك
- إذا لم يكن لديك حساب، قم بإنشاء واحد مجاناً

### **الخطوة 3: إنشاء API Key**

1. انقر على زر **"Create API Key"** أو **"إنشاء مفتاح API"**
2. اختر مشروع Google Cloud (أو أنشئ مشروع جديد)
3. انسخ المفتاح الذي تم إنشاؤه

### **الخطوة 4: إضافة المفتاح للنظام**

افتح ملف `.env` في المجلد الرئيسي وقم بتحديث السطر التالي:

```bash
GEMINI_API_KEY=your-actual-api-key-here
```

استبدل `your-actual-api-key-here` بالمفتاح الذي حصلت عليه.

### **الخطوة 5: إعادة تشغيل الخادم**

بعد إضافة المفتاح، قم بإعادة تشغيل الخادم:

```bash
# أوقف الخادم الحالي (Ctrl+C)
# ثم شغله مرة أخرى
npm run dev
```

---

## ✅ التحقق من التفعيل

عند تشغيل الخادم، يجب أن ترى الرسالة التالية:

```
✅ Gemini AI initialized successfully
```

إذا رأيت:
```
⚠️ Gemini API Key not configured. Using fallback search.
```

هذا يعني أن المفتاح غير مُعرّف أو غير صحيح.

---

## 🎯 المميزات المتوفرة

### **1. فهم ذكي للأسئلة**
- يفهم السياق والمعنى وليس فقط الكلمات المفتاحية
- يدعم اللغة العربية بشكل كامل

### **2. إجابات تفصيلية**
- يقدم شرح شامل مع خطوات عملية
- يستخدم المعلومات من قاعدة البيانات والكراسات الفنية

### **3. تحليل الكراسات الفنية**
- يقرأ محتوى ملفات PDF المرفوعة
- يستخرج المعلومات ذات الصلة

### **4. توصيات ذكية**
- يقترح حلول بناءً على السياق
- يربط بين المعلومات المختلفة

---

## 📊 مثال على الاستخدام

### **قبل Gemini (بحث نصي بسيط):**

**سؤال:** "لماذا الفرامل لا تعمل؟"

**جواب:**
```
🔍 من الكراسات الفنية:
عثرت على 2 كراسة تحتوي على معلومات ذات صلة.
```

### **بعد Gemini (إجابة ذكية):**

**سؤال:** "لماذا الفرامل لا تعمل؟"

**جواب:**
```
🔧 أسباب محتملة لعطل الفرامل:

1. **نقص زيت الفرامل:**
   - تحقق من مستوى زيت الفرامل في الخزان
   - ابحث عن تسريبات في الخطوط الهيدروليكية

2. **تآكل أقراص الفرامل:**
   - افحص سماكة الأقراص
   - استبدلها إذا كانت أقل من الحد الأدنى

3. **مشكلة في المضخة الرئيسية:**
   - افحص ضغط النظام الهيدروليكي
   - تحقق من عمل المضخة

📚 بناءً على الكراسات الفنية المتوفرة:
- راجع دليل الصيانة للفرامل (صفحة 45-52)
- اتبع إجراءات السلامة المذكورة

⚠️ ملاحظة: إذا استمرت المشكلة، قد تحتاج لفحص شامل للنظام.
```

---

## 💰 التكلفة والحدود

### **الاستخدام المجاني:**
- **60 طلب في الدقيقة**
- **1,500 طلب في اليوم**
- **1 مليون طلب في الشهر**

هذه الحدود كافية جداً للاستخدام العادي!

### **إذا احتجت المزيد:**
يمكنك الترقية إلى خطة مدفوعة، لكن الخطة المجانية كافية لمعظم الحالات.

---

## 🔄 وضع الاحتياطي (Fallback)

إذا لم يتم تفعيل Gemini API أو حدث خطأ:
- ✅ النظام سيعمل بشكل طبيعي
- ✅ سيستخدم البحث النصي البسيط
- ✅ لن يتوقف التطبيق

---

## 🛠️ استكشاف الأخطاء

### **المشكلة: "API Key not configured"**
**الحل:**
1. تأكد من إضافة المفتاح في ملف `.env`
2. تأكد من عدم وجود مسافات إضافية
3. أعد تشغيل الخادم

### **المشكلة: "Invalid API Key"**
**الحل:**
1. تأكد من نسخ المفتاح بشكل صحيح
2. تحقق من صلاحية المفتاح في Google AI Studio
3. جرب إنشاء مفتاح جديد

### **المشكلة: "Quota exceeded"**
**الحل:**
1. انتظر حتى يتم تجديد الحصة (كل دقيقة/يوم)
2. أو قم بالترقية لخطة مدفوعة

---

## 📞 الدعم

للمزيد من المعلومات:
- [Google AI Studio Documentation](https://ai.google.dev/docs)
- [Gemini API Pricing](https://ai.google.dev/pricing)

---

## ✨ ملاحظات مهمة

1. **الأمان:** لا تشارك API Key مع أحد
2. **الخصوصية:** البيانات المرسلة لـ Gemini تخضع لسياسة Google
3. **الأداء:** الاستجابة قد تستغرق 2-5 ثواني حسب حجم السؤال
4. **اللغة:** النظام مُحسّن للغة العربية بشكل كامل

---

تم التطوير بواسطة: **Intelligent Technical Assistant Team** 🚀
